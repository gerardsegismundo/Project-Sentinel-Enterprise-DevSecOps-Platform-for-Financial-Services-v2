skip-check:
  - CKV_TF_1
  - CKV_TF_2   # module source version tags — local modules, not remote
  - CKV_AWS_136 # ECR KMS encryption — requires KMS key provisioning (tracked)
  - CKV_AWS_158 # CloudWatch Log Group KMS encryption — requires KMS key provisioning (tracked)
  - CKV_AWS_338 # CloudWatch log retention — dev environment, will configure for prod
  - CKV_AWS_66  # CloudWatch log retention days — same as above
  - CKV_AWS_23  # Security group description — will address in security hardening
  - CKV_AWS_260 # Security group ingress 0.0.0.0/0 to port 80 — ALB requires public access
  - CKV_AWS_393 # OIDC claim validation — bootstrapping phase, will tighten
  - CKV_AWS_61  # IAM assume role across services — OIDC provider design
  - CKV_AWS_60  # IAM role principal restriction — same as above
  - CKV_AWS_274 # AdministratorAccess policy — bootstrap role, will scope down